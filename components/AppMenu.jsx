import React from "react";

// AppMenu is now replaced by bottom tab navigation in (tabs)/_layout.jsx
// This component is kept for backward compatibility but renders nothing

export default function AppMenu() {
  return null;
}
